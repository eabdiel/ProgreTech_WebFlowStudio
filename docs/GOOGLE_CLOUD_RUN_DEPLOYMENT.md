# Google Cloud Run deployment

No SAP Application Router, XSUAA, MTA, VCAP services, or Hub routes are required in this branch. Use Cloud Run plus optional GCS persistence. Keep max instances at 1 until persistence is moved to a transactional multi-instance design.
